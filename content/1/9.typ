#import "../../templates/templater.typ": *

When x is close to a, the tangent line ot f at $x=a$

#notation()[
  The approximation $f(x) approx L(x)$, or equivalently,
  $ f(x) approx f(a) + f'(a)dot(x-a) $
]

#theorem()[
  Let $y=f(x)$ and it is a differntiable funciton. To describe how the value of f changes by change of x, we use *differentials*

  Consider $dx$ as some sort of rate of change of x, and respectively for y.
  Then,

  $ dy = f'(x)dx $
  Somewhat like a generalization of *derivative*... we can't define precisely now.
]
